---
- name: Configure SDN topology on EC2
  hosts: all
  become: true

  vars:
    topology_dir: /home/ubuntu/sdn-topology
    topology_type: "{{ lookup('env', 'TOPOLOGY_DIR') | default('fat-tree', true) }}"

  roles:
    - sdn_topology
